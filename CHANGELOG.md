# 更新日志

## 2026-09-14

本次更新集中改善图库浏览、卡片盒编辑与图片整理体验。

### 图库显示与性能

- 卡片封面改用完整图片资源显示，减少高分辨率屏幕下的模糊感。
- 优化响应式列数计算：根据窗口可用宽度自动分配卡片，宽屏默认可容纳更多列，同时保留手动调整入口。
- 优化图片缓存、Blob URL 释放与卡片局部刷新，降低大图库连续浏览时的资源占用。
- 顶部模型栏和工具栏支持吸顶，滚动浏览时仍可快速切换图库、搜索和调整显示。

### 添加与管理图片

- 支持从外部窗口直接拖入图片到卡片盒，自动完成添加。
- 卡片右上角统一为操作菜单，包含“编辑卡片盒”“整理图片”“添加图片”“删除当前图片”和“删除卡片盒”。
- 取消图片悬停即删除的高风险操作；删除图片和卡片盒改为明确入口，并增加确认提示。
- 空卡片盒保留清晰的删除入口，避免图片删空后难以移除卡片盒。

### 编辑卡片盒

- 正面提示词区域扩大并保持常显，便于选取部分内容或整体复制。
- 新增提示词复制按钮：存在文本选区时复制选中内容，否则复制全部正面提示词。
- 当前图片新增单张删除按钮，并提供默认关闭的“Delete 快删”开关；输入框内不会触发快捷删除。
- 高级信息默认折叠，降低不常用字段对主要编辑流程的干扰。
- 删除卡片盒按钮缩小并降低视觉强调，减少误触。

### 盒内图片整理

- 从卡片三点菜单可直接进入“整理图片”，不再经过编辑页二次跳转。
- 支持拖动图片调整盒内顺序，也可使用左右方向键进行精细移动。
- 排序缩略图按原图比例完整显示，未填满区域留空，方便判断画面内容。
- 每张排序缩略图右下角提供单图删除按钮。
- 整理过程中可点击图片放大，并通过按钮、键盘方向键或横向滑动连续查看大图。
- 保存排序后保留当前图片位置；取消排序则不写入顺序变更。

### 其他交互调整

- 顶部“显示设置”与其他操作项重新对齐，统一工具栏节奏。
- 切换图库分组时保留搜索、标签和排序条件。
- 改进复制、删除、保存等操作的状态反馈。

> 数据仍保存在浏览器本地 IndexedDB 中，本次更新不需要迁移已有图库数据。升级前仍建议通过侧边栏导出 zip 备份重要图库。
